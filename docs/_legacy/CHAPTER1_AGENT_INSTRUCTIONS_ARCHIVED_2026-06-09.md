# Archived Chapter 1 Agent Instructions

**Historical status:** superseded on 2026-06-09.

The former top-level `AGENTS.md` and `CLAUDE.md` described this repository as
the Chapter 1 critical replication of Shaikh (2016), with locked series
`KGCcorp`, `pIGcorpbea`, `VAcorp`, and sample 1947-2007. They also described
S0/S1/S2 ARDL and VECM stages and prohibited changes to the former
`codes/10_config.R` configuration.

Those instructions remain relevant only when reading historical commits and
the scripts now located under:

```text
codes/_legacy/provider_prelock_2026-06-09/
```

They are not binding for the active BEA-provider task. The active contract is
defined by top-level `AGENTS.md` and
`docs/US_BEA_VARIABLE_MENU_PROVIDER_CONTRACT.md`.

No historical raw or processed data was deleted during the provider lock.
